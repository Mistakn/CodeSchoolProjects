@page
@model MyOnlinePetStoreWeb.Areas.Identity.Pages.MyAccount.LoginModel

<div class="row">
    <div class="col-md-12">
        <section>
            <form id="account" method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group my-3">
                    <label asp-for="Input.Email"></label>
                    <input asp-for="Input.Email" class="form-control" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-group my-3">
                    <label asp-for="Input.Password"></label>
                    <input asp-for="Input.Password" class="form-control" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="form-group my-3">
                    <button type="submit" class="btn btn-primary">Log in</button>
                </div>
            </form>
        </section>
    </div>
</div>

@*<div class="row text-center">
        <span>Or</span>
    </div>

    <div class="row">
        <div class="col-12 d-grid">
            <form method="post" action="~/Identity/MyAccount/ExternalLogin">
                @foreach (var provider in Model.ExternalLogins) {
                    <button class="btn btn-primary" name="provider" value="@provider.Name">@provider.DisplayName</button>
                }
            </form>
        </div>
    </div>*@

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
